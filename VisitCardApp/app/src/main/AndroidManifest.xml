<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.unice.visitcardapp">

    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".CardCreatedActivity"
            android:label="@string/card_create">
        </activity>

        <activity
            android:name=".CreateActivity"
            android:label="@string/card_create">
        </activity>

        <activity
            android:name=".CreateFromQRActivity"
            android:label="@string/create_from_qr">
        </activity>

        <activity
            android:name=".CreateNewCardOrEditActivity"
            android:label="@string/create_from_text_input">
        </activity>

        <activity
            android:name=".DisplayActivity"
            android:label="@string/card_display">
        </activity>

        <activity
            android:name=".DisplayVisitCardActivity"
            android:label="@string/card_display">
        </activity>

    </application>

</manifest>